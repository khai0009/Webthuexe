@model IEnumerable<Webthuexe.Models.SOGHE>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPage1.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SOGHE1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SOGHE1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MASG }) |
            @Html.ActionLink("Details", "Details", new { id=item.MASG }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MASG })
        </td>
    </tr>
}

</table>
